---
title: '图生视频-首尾图模式-查询任务'
api: 'GET https://ai-nebula.com/prod-api/nebula/v4/api/textOrImage2videoQuery/{taskId}'
---

## 简介

查询首尾图模式图生视频任务状态和结果。

## 认证

<ParamField header="Authorization" type="string" required>
  Bearer Token，如 `Bearer eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...`
  获取方法：依照左侧最上面认证模块的post接口获取，`https://docs.openai-nebula.com/cn/nebula-lab-v4/endpoint/get-token`
</ParamField>

## 请求参数

<ParamField path="taskId" type="string" required>
  任务ID，由提交任务接口返回
</ParamField>

### 示例代码

<Tabs>
  <Tab title="cURL示例">
    ```bash
    curl -X GET "https://ai-nebula.com/prod-api/nebula/v4/api/textOrImage2videoQuery/task-1234567890" \
      -H "Authorization: Bearer sk-xxxxxxxxxx"
    ```
  </Tab>
  <Tab title="Python示例">
    ```python
    import requests

    task_id = "task-1234567890"
    url = f"https://ai-nebula.com/prod-api/nebula/v4/api/textOrImage2videoQuery/{task_id}"
    headers = {"Authorization": "Bearer sk-xxxxxxxxxx"}

    response = requests.get(url, headers=headers)
    print(response.json())
    ```
  </Tab>
</Tabs>

### 响应示例

<ResponseExample>
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "id": "task-1234567890",
    "status": "succeeded",
    "url": "https://example.com/video.mp4"
  }
}
```
</ResponseExample>