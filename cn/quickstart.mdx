---
title: '快速开始'
description: '只需简单三步，即可开始使用 Nebula Api 访问各种 AI 模型'
---

## 第一步：账户准备

### 1.1 注册账号

访问 [Nebula Api 官网](https://ai-nebula.com) 注册账号：
- 使用手机号注册并完成验证
- 登录控制台

### 1.2 账户充值

在控制台进行充值：
1. 点击「个人中心」
2. 点击「费用充值」，完成支付（支持支付宝、微信）

<Note>
新用户注册额外赠送 1 美金
</Note>

## 第二步：创建 API 密钥

### 2.1 生成密钥

1. 在后台导航点击「模型中心」
2. 点击「API 密钥」
3. 点击「新建」生成密钥


## 第三步：开始调用

### 3.1 获取接入信息

| 配置项 | 值 |
|-------|-----|
| **API 地址（Base URL）** | `https://llm.ai-nebula.com/v1` |
| **API 密钥** | 您创建的密钥 |
| **请求格式** | 完全兼容 OpenAI API |

### 3.2 测试调用

使用 curl 快速测试：

```bash
curl https://llm.ai-nebula.com/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '{
    "model": "gpt-5.4",
    "messages": [{"role": "user", "content": "Hello!"}]
  }'
```

### 3.3 代码示例

<Tabs>
  <Tab title="Python">
    ```python
    from openai import OpenAI

    client = OpenAI(
        api_key="YOUR_API_KEY",
        base_url="https://llm.ai-nebula.com/v1"
    )

    response = client.chat.completions.create(
        model="gpt-5.4",
        messages=[
            {"role": "user", "content": "Hello!"}
        ]
    )

    print(response.choices[0].message.content)
    ```
  </Tab>
  <Tab title="Node.js">
    ```javascript
    import OpenAI from 'openai';

    const openai = new OpenAI({
      apiKey: 'YOUR_API_KEY',
      baseURL: 'https://llm.ai-nebula.com/v1'
    });

    const response = await openai.chat.completions.create({
      model: 'gpt-5.4',
      messages: [{ role: 'user', content: 'Hello!' }]
    });

    console.log(response.choices[0].message.content);
    ```
  </Tab>
  <Tab title="Java">
    ```java
    OpenAiService service = new OpenAiService(
        "YOUR_API_KEY", 
        Duration.ofSeconds(60), 
        "https://llm.ai-nebula.com/v1"
    );

    ChatCompletionRequest request = ChatCompletionRequest.builder()
        .model("gpt-5.4")
        .messages(List.of(
            new ChatMessage(ChatMessageRole.USER, "Hello!")
        ))
        .build();

    ChatCompletionResult result = service.createChatCompletion(request);
    System.out.println(result.getChoices().get(0).getMessage().getContent());
    ```
  </Tab>
</Tabs>

## 下一步

恭喜！您已经成功完成了 Nebula Api 的接入。接下来您可以：

<Columns cols={2}>
  <Card title="查看 API 文档" icon="book" href="/cn/api-reference/models">
    了解完整的 API 接口说明
  </Card>
  <Card title="集成到应用" icon="puzzle-piece" href="/cn/integrations/cursor">
    将 Nebula Api 集成到各种工具
  </Card>
</Columns>

## 常见问题

<AccordionGroup>
  <Accordion title="如何切换模型？">
    只需修改请求中的 `model` 参数：
    ```json
    {
      "model": "gpt-5.4",
      "model": "claude-sonnet-4-6",
      "model": "gemini-3.1-pro-preview"
    }
    ```
  </Accordion>
  <Accordion title="支持哪些编程语言？">
    Nebula Api 兼容 OpenAI API 标准，支持所有 OpenAI SDK 支持的语言：
    - Python
    - JavaScript/TypeScript
    - Java
    - C#/.NET
    - Go
    - Ruby
    - PHP
  </Accordion>
  <Accordion title="如何查看余额？">
    登录控制台即可查看：
    - 账户余额
    - 使用记录
    - 消费统计
  </Accordion>
  <Accordion title="遇到问题怎么办？">
    1. 查看 API 文档
    2. 检查常见错误
    3. 联系客服：mla@nebula-data.com
  </Accordion>
</AccordionGroup>

<Tip>
保存好您的 API 密钥，并定期在控制台查看使用日志，合理优化成本。祝使用愉快~
</Tip>
