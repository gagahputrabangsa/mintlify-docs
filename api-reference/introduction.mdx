---
title: "Plant Store API Reference — Endpoints & Authentication"
sidebarTitle: "Introduction"
description: "Learn how to authenticate and call the Plant Store API. Browse all four endpoints — list, create, delete, and webhook — with examples and response schemas."
---

The Plant Store API is a sample REST API that demonstrates how Mintlify renders interactive API reference documentation from an [OpenAPI specification](https://mintlify.com/docs/api-playground/openapi/setup). Every endpoint shown here is generated automatically from `api-reference/openapi.json` — no hand-written API docs needed.

<Note>
  This API reference is an example. To build your own API reference, replace `api-reference/openapi.json` with your own OpenAPI spec and update the navigation in `docs.json`.
</Note>

## Authentication

All Plant Store API endpoints require a Bearer token passed in the `Authorization` header:

```bash
curl -H "Authorization: Bearer YOUR_API_TOKEN" \
     http://sandbox.mintlify.com/plants
```

Mintlify reads the Bearer scheme from the OpenAPI spec and adds an **Authorization** field to the interactive API playground automatically — readers can enter their token once and test all endpoints without leaving the docs.

## Base URL

```
http://sandbox.mintlify.com
```

## Endpoints

<CardGroup cols={2}>
  <Card title="List Plants" icon="leaf" href="/api-reference/endpoint/get">
    `GET /plants` — Retrieve all plants you have access to.
  </Card>
  <Card title="Create Plant" icon="seedling" href="/api-reference/endpoint/create">
    `POST /plants` — Add a new plant to the store.
  </Card>
  <Card title="Delete Plant" icon="trash" href="/api-reference/endpoint/delete">
    `DELETE /plants/{id}` — Remove a plant by its ID.
  </Card>
  <Card title="Plant Webhook" icon="webhook" href="/api-reference/endpoint/webhook">
    `WEBHOOK /plant/webhook` — Receive notifications when a plant is added.
  </Card>
</CardGroup>
