---
title: "Add a New Plant to the Plant Store — POST /plants API"
sidebarTitle: "Create Plant"
description: "Create a new plant record in the Plant Store. Send a JSON body with the plant's id, name, and an optional tag. Returns the created Plant object."
openapi: "POST /plants"
---
